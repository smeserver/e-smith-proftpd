Index: e-smith-proftpd/createlinks
diff -u e-smith-proftpd/createlinks:1.7 e-smith-proftpd/createlinks:1.8
--- e-smith-proftpd/createlinks:1.7	Fri Sep  5 15:41:58 2003
+++ e-smith-proftpd/createlinks	Mon Aug 30 16:34:50 2004
@@ -7,115 +7,33 @@
 service_link_enhanced("ftp", "K25", "6");
 service_link_enhanced("ftp", "K25", "0");
 
-#--------------------------------------------------
-# actions for console-save event
-#--------------------------------------------------
-my $event = "console-save";
+foreach my $event (qw(
+    console-save
+    bootstrap-console-save
+    domain-create
+    domain-delete
+    domain-modify
+    ibay-create
+    ibay-delete
+    ibay-modify
+    ibay-modify-servers
+    ip-change
+    network-create
+    network-delete
+    remoteaccess-update
+    password-modify
+    user-create
+    user-delete))
+{
+    event_link("proftpd-conf", $event, "47");
+}
+
+foreach my $event (qw(
+    remoteaccess-update
+    user-create
+    user-delete))
+{
+    event_link("proftpd-startstop", $event, "57");
+}
 
-event_link("proftpd-conf", $event, "47");
-
-#--------------------------------------------------
-# actions for bootstrap-console-save event
-#--------------------------------------------------
-$event = "bootstrap-console-save";
-
-event_link("proftpd-conf", $event, "47");
-
-#--------------------------------------------------
-# actions for domain-create event
-#--------------------------------------------------
-
-$event = "domain-create";
-
-event_link("proftpd-conf", $event, "47");
-
-#--------------------------------------------------
-# actions for domain-delete event
-#--------------------------------------------------
-
-$event = "domain-delete";
-
-event_link("proftpd-conf", $event, "47");
-
-#--------------------------------------------------
-# actions for domain-modify event
-#--------------------------------------------------
-
-$event = "domain-modify";
-
-event_link("proftpd-conf", $event, "47");
-
-#--------------------------------------------------
-# actions for ibay-create event
-#--------------------------------------------------
-
-$event = "ibay-create";
-
-event_link("proftpd-conf", $event, "47");
-
-#--------------------------------------------------
-# actions for ibay-delete event
-#--------------------------------------------------
-
-$event = "ibay-delete";
-
-event_link("proftpd-conf", $event, "47");
-
-#--------------------------------------------------
-# actions for ibay-modify event
-# (used after changing parameters for a single i-bay)
-#--------------------------------------------------
-
-$event = "ibay-modify";
-
-event_link("proftpd-conf", $event, "47");
-
-#--------------------------------------------------
-# actions for ibay-modify-servers event
-# (used after changing the group of several i-bays in sequence)
-#--------------------------------------------------
-
-$event = "ibay-modify-servers";
-
-event_link("proftpd-conf", $event, "47");
-
-#--------------------------------------------------
-# actions for ip-change event
-#--------------------------------------------------
-
-$event = "ip-change";
-
-event_link("proftpd-conf", $event, "47");
-
-#--------------------------------------------------
-# actions for network-create event
-#--------------------------------------------------
-
-$event = "network-create";
-
-event_link("proftpd-conf", $event, "47");
-
-#--------------------------------------------------
-# actions for network-delete event
-#--------------------------------------------------
-
-$event = "network-delete";
-
-event_link("proftpd-conf", $event, "47");
-
-#--------------------------------------------------
-# actions for remoteaccess-update event
-#--------------------------------------------------
-
-$event = "remoteaccess-update";
-
-event_link("proftpd-conf", $event, "47");
-event_link("proftpd-startstop", $event, "57");
-
-#--------------------------------------------------
-# actions for password-modify event
-#--------------------------------------------------
-
-$event = "password-modify";
-
-event_link("proftpd-conf", $event, "47");
+exit 0;
Index: e-smith-proftpd/e-smith-proftpd.spec
